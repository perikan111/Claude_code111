# クイックスタートガイド

## 🚀 今すぐ試す（3ステップ）

### ステップ1: GitHub Pagesを有効化

1. このリポジトリの **Settings** タブを開く
2. 左メニューから **Pages** を選択
3. **Source** で以下を設定：
   - Branch: **`main`**
   - Folder: **`/docs`**
4. **Save** をクリック

### ステップ2: デプロイ完了を待つ

数分後、以下のURLでアクセス可能になります：

```
https://<YOUR_USERNAME>.github.io/<REPO_NAME>/
```

### ステップ3: ゲーム開始！

1. URLにアクセス
2. モデルのダウンロード完了を待つ（初回のみ2〜5分）
3. チャット入力欄に行動を入力
4. GMの応答を楽しむ

---

## 🎮 使い方

### 基本操作

**入力例：**
```
「酒場に入る」
「城門の衛兵に話しかける」
「冒険者ギルドに向かう」
```

**GMの応答例：**
```
あなたは「黄金のグリフォン」酒場に足を踏み入れます。
店内は冒険者で賑わっており、カウンターには白髪の老婆マーサが
立っています。隅のテーブルには傷だらけの戦士が座り、
赤いローブの魔術師が本を読んでいます。

誰に話しかけますか？
```

### 初回起動

初回アクセス時は以下の画面が表示されます：

1. **「モデルを読み込んでいます...」**
   - Microsoft Phi-3.5モデル（約2.5GB）をダウンロード
   - 光回線で2〜5分程度
   - プログレスバーで進捗確認

2. **「準備完了」**
   - ダウンロード完了
   - モデルはブラウザキャッシュに保存
   - チャット入力が有効になる

### 2回目以降

ブラウザキャッシュからモデルを読み込むため、**数秒で起動**します。

---

## 🔧 動作環境

### 推奨ブラウザ

- ✅ **Chrome** 最新版（推奨）
- ✅ **Edge** 最新版
- ❌ Firefox（WebGPU未対応）
- ❌ Safari（WebGPU未対応）

### システム要件

| 項目 | 最小要件 | 推奨 |
|------|----------|------|
| メモリ | 4GB | 8GB以上 |
| GPU | なし（CPUでも動作） | あり（高速） |
| ストレージ | 3GB以上の空き | 5GB以上 |
| ネット回線 | 初回のみ必要 | 光回線推奨 |

### WebGPU確認方法

Chrome/Edgeで `chrome://gpu` にアクセスし、以下を確認：

```
Graphics Feature Status
WebGPU: Hardware accelerated
```

**Hardware accelerated** と表示されればOK！

---

## 💡 よくある質問

### Q: APIキーは必要ですか？

**A: 不要です。** すべてブラウザ内で動作します。

### Q: 課金されませんか？

**A: 一切課金されません。** GitHub Pagesは無料、WebLLMも無料です。

### Q: データはどこに保存されますか？

**A: ブラウザのキャッシュのみです。** サーバーには送信されません。

### Q: オフラインで使えますか？

**A: はい。** モデルダウンロード後はオフラインで動作します。

### Q: スマホで使えますか？

**A: 現状厳しいです。** WebGPU対応が必要なため、PCブラウザ推奨。

### Q: モデルのダウンロードに失敗します

**A: 以下を確認してください：**
1. Chrome/Edge 最新版を使用
2. インターネット接続が安定している
3. ストレージに3GB以上の空きがある
4. プライベートモードではない

### Q: 応答が遅いです

**A: 以下が原因の可能性があります：**
1. GPUがない（CPUのみで動作）
2. メモリ不足（8GB推奨）
3. 初回生成（2回目以降は速い）

### Q: キャッシュをクリアしたい

**A: ブラウザ設定から：**
1. Chrome: 設定 → プライバシーとセキュリティ → 閲覧履歴データの削除
2. 「キャッシュされた画像とファイル」のみ選択
3. 削除後、再度モデルをダウンロード

---

## 🎨 カスタマイズ

### ドキュメントの追加

`docs/data/documents.js` を編集：

```javascript
export const trpgDocuments = [
    // 既存のドキュメント...
    {
        id: 'custom-1',
        text: 'あなたのカスタム設定...',
        metadata: { type: 'custom', category: 'world' }
    }
];
```

### UIのカスタマイズ

`docs/style.css` を編集して、色やレイアウトを変更できます。

---

## 🐛 トラブルシューティング

### エラー: "WebGPU not supported"

**解決策：**
1. Chrome/Edgeの最新版に更新
2. `chrome://flags` で WebGPU を有効化
3. GPUドライバーを最新に更新

### エラー: "Failed to download model"

**解決策：**
1. インターネット接続を確認
2. ブラウザキャッシュをクリア
3. 別のネットワークで試す
4. VPNを無効化

### 応答が途中で止まる

**解決策：**
1. ページをリロード
2. メモリ不足の可能性（他のタブを閉じる）
3. ブラウザを再起動

### 日本語がおかしい

**解決策：**
- Phi-3.5は多言語対応ですが、英語が最適
- プロンプトを調整してください

---

## 📚 次のステップ

### 学習リソース

- [WebLLM公式ドキュメント](https://webllm.mlc.ai/)
- [Microsoft Phi-3.5モデル](https://huggingface.co/microsoft/Phi-3.5-mini-instruct)
- [WebGPU入門](https://www.w3.org/TR/webgpu/)

### 機能拡張アイデア

1. **セッション保存**: IndexedDBで会話履歴保存
2. **キャラクター作成**: プレイヤーキャラのシート管理
3. **ダイスロール**: d20システムの実装
4. **マルチプレイヤー**: WebRTCで複数人対応
5. **音声入出力**: Web Speech API統合

### コミュニティ

- GitHubで Issue/PR歓迎
- 改善案があればぜひ共有してください

---

## ⚡ パフォーマンスTips

### 高速化

1. **GPU使用**: WebGPU対応GPUで10倍以上高速化
2. **メモリ確保**: 他のアプリを閉じる
3. **ブラウザ最適化**: ハードウェアアクセラレーションを有効化

### メモリ節約

1. **他のタブを閉じる**
2. **拡張機能を無効化**
3. **ブラウザ再起動**

---

## 🎉 楽しいTRPGを！

質問や問題があれば、GitHubのIssuesでお気軽にどうぞ！
